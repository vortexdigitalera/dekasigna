set timeout=5
set default=0

menuentry "DEKASIGNA UEFI Lab (amd64 / Intel / Non-CSM)" {
  echo "DEKASIGNA firmware lab booted in UEFI mode."
  echo "Primary target: amd64/x86_64, Intel, UEFI-only, no CSM fallback."
  echo "Use the copied toolkit files and scripts for Secure Boot and firmware lab work."
  sleep 2
}

menuentry "Open DEKASIGNA toolkit documents" {
  echo "Read README.txt and the labs/ and windows/ folders from the ISO filesystem."
  echo "This image is designed for futuristic UEFI-first firmware experiments."
  sleep 2
}
